<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.paymentdemo.mapper.OrderInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.paymentdemo.entity.OrderInfo">
        <result column="title" property="title" />
        <result column="order_no" property="orderNo" />
        <result column="user_id" property="userId" />
        <result column="product_id" property="productId" />
        <result column="total_fee" property="totalFee" />
        <result column="code_url" property="codeUrl" />
        <result column="order_status" property="orderStatus" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        title, order_no, user_id, product_id, total_fee, code_url, order_status
    </sql>

</mapper>
